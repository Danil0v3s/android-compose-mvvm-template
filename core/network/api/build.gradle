plugins {
    id("com.android.library")
    id("kotlin-android")
}

dependencies {
    api(libs.okhttp)
    api(platform(libs.retrofit.bom))

    implementation(libs.hilt)
}

android {
    namespace 'br.com.firstsoft.core.network.api'
}
